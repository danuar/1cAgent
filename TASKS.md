# Задачи проекта 1С-агента

Формат записи: `- [дата] суть. Контекст/где упёрлись.` Одна строка на пункт; подробности — в
`HANDOFF_ARCHIVE.md` под номером тикета. Закрытое переносить в «Закрыто» одной строкой.

## Открыто
- [2026-09-15] `adopt_object` — только «пустое» заимствование `Document`/`Catalog`. Не закрыто: перехват процедур модуля объекта (не формы), заимствование других видов, добавление СОВСЕМ НОВОЙ команды формы. Формат не гадать — нужен реальный дамп от человека.
- [2026-09-15] `deploy_report`/`deploy_form` на новых (не переисследованных) кейсах живьём не проверены.
- [2026-09-15] `install.ps1`: ветки «LM Studio не найдена» и `MODEL_BACKEND=llama_server` не проверены (нет машины без LM Studio).
- [2026-09-15] `deploy_extension_from_files` может падать на рассинхроне версии формата у нетронутых файлов стороннего дампа. Обход — `sync_extension_files`.
- [2026-09-15] Сокращение docstring'ов инструментов сделано (78k → см. замер ниже). Дальше резать только если Desktop всё ещё тяжёлый на старте.
- [2026-09-15] Логирование троек «код → ошибка → фикс» для дообучения локальной модели — не начато, не приоритет.

## Чего не хватило в инструментах (пишет ИИ по ходу задач)
Когда упёрся в ограничение инструмента, сделал что-то руками в обход или потратил >3 ходов на то,
что должен был делать один вызов — запиши сюда. Формат: `- [дата] инструмент/тема: чего не хватило → что сделал вместо.`

- (пусто)

## Намеренно НЕ делаем
- Прогрев моделей/кэш сборки при старте сервера — повторит #62 (автозагрузка моделей на каждый чат).
- Векторная СУБД для семантического индекса — numpy-хранилище достаточно.
- Смена модели-фиксера — Qwen2.5-Coder-7B под роль фикса достаточно (#38, #54).

## Закрыто (кратко, детали в архиве по номеру)
- #82–#91: `preflight`, `patch_epf_form_module`, `run_module_batch`, клиентское исполнение `on_client`, переименование обработок, битый первый символ вывода (#87), `Возврат` ломал быстрый путь (#84), два процесса mcp_server (#83).
- #70–#81: HTTP-транспорт раннера + DuckDNS/Caddy наружу, адаптивный опрос, отзывчивость сеанса.
- #64–#69: быстрый путь run_module без сборки .epf, сборка .epf с формами, run_watched на сборке, линт-гейт записи модуля.
- #60–#62: ленивый автозапуск моделей, межпроцессный замок.
- #57–#59: `install.ps1`.
- #52–#56: `adopt_form`, семантический поиск, живые тесты пакета #45–52, /IBName-матчинг.
- #21–#45: см. `HANDOFF_ARCHIVE.md`.
